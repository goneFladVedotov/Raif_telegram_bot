version: '3'

services:
  app:
    image: gonefladvedotov/payment_app:1.5
    build:
      context: .
      dockerfile: Dockerfile
    container_name: payment_app
    ports:
      - '8081:8081'
    environment:
      - SERVER_PORT= 8081